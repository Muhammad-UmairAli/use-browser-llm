---
name: Public API polish, type exports & npm publish config/CI
status: closed
created: 2026-07-07T11:47:41Z
updated: 2026-07-07T11:47:41Z
github: https://github.com/Muhammad-UmairAli/use-local-llm/issues/20
depends_on: [8, 9]
parallel: false
conflicts_with: []
---

# Task: Public API polish, type exports & npm publish config/CI

## Description

Final pre-publish pass: audit the full public API surface for consistency and no leaked `web-llm`/Comlink internals, ensure every exported symbol has a shipped `.d.ts` type with no `any`, verify `sideEffects: false` tree-shaking and worker-chunk code-splitting hold under a real bundler consumer test, and wire the npm publish CI workflow (gated on a version tag, not auto-run).

## Acceptance Criteria
- [ ] No `web-llm` or `comlink` types appear in the public API surface (verified by an `api-extractor`-style check or manual review of the `.d.ts` output)
- [ ] `npm pack` output contains only `dist/`, `README.md`, `LICENSE`, and `package.json` — no source, no test files, no worker source maps unless intentionally shipped
- [ ] A throwaway consumer project importing the built package confirms the worker chunk is not included in the main bundle (bundle-size assertion)
- [ ] Publish CI workflow exists and is gated on a `v*` tag push; it is not wired to run on every merge to `develop`/`main`

## Technical Details

This is the wrap-up task — it depends on the test suite (008) being green and the docs (009) being final, since both inform what "the public API" actually is by this point.

## Dependencies

Depends on task 008 (tests green) and task 009 (docs final — the documented API is the API being polished/frozen here).

## Effort Estimate
- Size: L
- Hours: 7.28

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
