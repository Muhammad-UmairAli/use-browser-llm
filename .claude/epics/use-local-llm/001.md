---
name: Package scaffolding & build tooling
status: open
created: 2026-07-07T11:47:41Z
updated: 2026-07-07T11:47:41Z
github: (will be set on sync)
depends_on: []
parallel: false
conflicts_with: []
---

# Task: Package scaffolding & build tooling

## Description

Scaffold the npm package: TypeScript strict config, `tsup` build producing dual ESM/CJS output for the main entry plus a separate code-split worker chunk, `package.json` `exports` map, `sideEffects: false`, lint/format config, and CI skeleton (lint + typecheck, no publish yet).

## Acceptance Criteria
- [ ] `package.json` has a valid dual ESM/CJS `exports` map and `sideEffects: false`
- [ ] `tsup` config builds `dist/index.{js,cjs,d.ts}` and a separate `dist/worker.{js,d.ts}` chunk
- [ ] `tsc --noEmit` passes under strict mode on an empty/stub source tree
- [ ] CI workflow runs lint + typecheck on every PR

## Technical Details

Stack A confirmed: `@mlc-ai/web-llm` + `comlink` as the only runtime deps; `tsup` as the bundler; Vitest as the test runner (wired in task 008, but the config file can be stubbed here).

## Dependencies

None — this is the foundation task.

## Effort Estimate
- Size: L
- Hours: 1.04

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
