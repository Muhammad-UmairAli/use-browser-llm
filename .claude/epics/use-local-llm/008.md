---
name: Unit tests (mocked engine) + worker-boundary integration harness
status: closed
created: 2026-07-07T11:47:41Z
updated: 2026-07-07T11:47:41Z
github: https://github.com/Muhammad-UmairAli/use-browser-llm/issues/18
depends_on: [4, 5, 6, 7]
parallel: false
conflicts_with: []
---

# Task: Unit tests (mocked engine) + worker-boundary integration harness

## Description

Round out test coverage across the hook's full surface: a mocked-engine Vitest suite that exercises every state transition, generation path, cache-status branch, and error/fallback path from tasks 004–007 without requiring a real GPU, plus an integration harness that verifies the worker boundary is real (inference genuinely runs off the main thread) rather than mocked away entirely.

## Acceptance Criteria
- [ ] Mocked-engine suite covers: idle→loading→ready, idle→unsupported, load failure→error→recovery, generate/streamGenerate happy path, abort mid-generation, cache hit vs cache miss
- [ ] Suite runs in CI without a GPU (fully mocked engine, no real WebGPU dependency)
- [ ] A separate integration test (may be browser/Playwright-driven, allowed to be skipped in CI if no WebGPU runner is available, but must exist and be runnable manually) verifies the worker boundary is genuinely used
- [ ] Full suite passes; no skipped/pending tests left unresolved

## Technical Details

This task does not add new product code — it is the test-completeness gate for tasks 004–007 before docs and publish work begins.

## Dependencies

Depends on tasks 004, 005, 006, 007 (everything under test must exist first).

## Effort Estimate
- Size: L
- Hours: 2.72

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
